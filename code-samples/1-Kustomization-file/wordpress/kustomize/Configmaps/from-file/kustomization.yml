apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  version: techtalk-cm

configMapGenerator:
  - name: nginx-config
    files:
      - nginx.conf

namePrefix: techtalk-cm-

resources:
- nginx-deployment.yml